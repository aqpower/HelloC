#include <bits/stdc++.h>
using namespace std;
int dic[10005];
vector<int> dp[105];
int main(){
    int n, m, t;
    cin >> n >> m;
    for (int i = 0; i < n;i++){
        cin >> t;
        dic[i] = t;
    }
    dp[0].push_back(0);
    for (int i = 1; i <= m;i++){
        for (int j = i; j < i;j++){
            
        }
    }

        return 0;
}