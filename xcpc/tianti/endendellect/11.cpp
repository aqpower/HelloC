#include <bits/stdc++.h>
using namespace std;

int main(){
    string a, t;
    while(getline(cin,t)){
        a += t;
    }
    int nword = 0;
    bool vis[a.size()] = {false};
    map<string, int> ans;
    for (int i = 0; i < a.size();i++){
        if(!vis[i]){
            
        }
    }

        return 0;
}