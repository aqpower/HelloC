#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e5 + 5;
int n;
typedef pair<int, int> P;
P dat[maxn];
int main(){
    cin >> n;
    for (int i = 0; i < n;i++){
        cin >> dat[i].first >> dat[i].second;
    }
    
    return 0;   
}