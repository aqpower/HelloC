#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int,int> P;
const int maxn = 1e5 + 5;

int main(){
    cin.tie(0)->ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while(t--){
        int n, m, k;
        cin >> n >> m >> k;
        int xs, ys, xt, yt;
        cin >> xs >> ys >> xt >> yt;
        for (int i = 0; i < n;i++){
            
        }
    }
    return 0;
}
